/dist
/seed.secret

# Generated by scripts/gen-version-file.sh (also run inside the Docker builder).
# Committing it pins every later build to one machine's stamp: version.Get()
# trusts a non-placeholder value and skips the ReadBuildInfo fallback entirely.
/internal/cmd/version/version.g.go

# README.md §Build tells people to build here; a stray 13 MB binary is one
# `git add -A` away from a permanently-cached module version.
/usersync
/usersync.exe

# The sidecar lock `usersync member` takes beside the roster. It is created in
# the roster's directory, which for an operator is a git checkout.
roster.yaml.lock
*.yaml.lock
